import { FilterRiskPipe } from './filter-risk.pipe';

describe('FilterRiskPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRiskPipe();
    expect(pipe).toBeTruthy();
  });
});
